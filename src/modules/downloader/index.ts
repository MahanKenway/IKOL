// Downloader module - Cloudflare Workers compatible stub
import { Composer } from 'grammy';
const bot = new Composer();
bot.command('download', (ctx) => ctx.reply('Download feature coming soon.'));
export default bot;
