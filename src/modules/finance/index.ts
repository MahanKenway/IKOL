// Finance module - Cloudflare Workers compatible stub
import { Composer } from 'grammy';
const bot = new Composer();
bot.command('finance', (ctx) => ctx.reply('Finance feature coming soon.'));
export default bot;
